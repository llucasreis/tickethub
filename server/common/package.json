{
  "name": "@lprs-tickethub/common",
  "version": "1.0.2",
  "main": "./build/index.js",
  "types": "./build/index.d.ts",
  "files": [
    "build/**/*"
  ],
  "license": "MIT",
  "scripts": {
    "build": "yarn clean && tsc",
    "clean": "del ./build/*",
    "patch": "version --patch",
    "publish": "publish --access public",
    "push": "yarn build && yarn patch && yarn publish"
  },
  "devDependencies": {
    "del-cli": "^3.0.1",
    "typescript": "^4.1.3"
  },
  "dependencies": {
    "@types/cookie-session": "^2.0.42",
    "@types/express": "^4.17.10",
    "@types/jsonwebtoken": "^8.5.0",
    "cookie-session": "^1.4.0",
    "express": "^4.17.1",
    "express-validator": "^6.9.2",
    "jsonwebtoken": "^8.5.1"
  }
}
